version: '3.3'

services:
  myubuntu:
    image: myubuntu
    container_name: myubuntu
#    command: /root/run/init.sh
    command: /usr/sbin/sshd -D
    ports:
      - '10022:22'
    expose:
      - '10022'
